var x = Number(prompt("Informe um valor inteiro positivo maior que zero:"));
var nomeCompleto = prompt("Informe o seu nome completo:");

for (var i = 1; i <= x; i++) {
    document.write(nomeCompleto + "<br>");
}
